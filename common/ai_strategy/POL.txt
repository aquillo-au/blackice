# befriend
# conquer
# antagonize
# build_ship
# build_army
# unit_ratio
# build_building
# research_tech
# garrison
# protect
# influence

stay_in_polish_faction = {
	enable = {
		OR = {
			tag = EST
			tag = LIT
			tag = LAT
			tag = ROM
			tag = CZE
			tag = FIN
			tag = YUG
		}
		democratic_govt = yes
		is_in_faction_with = POL
		
	}
	abort = {
		NOT = { is_in_faction_with = POL }
	}
	
	ai_strategy = {
        type = alliance
        id = "POL"     
        value = 300000
    }
}

british_help_against_germany = {
	enable = {
		tag = POL
		OR = {
			GER = { has_completed_focus = GER_second_vienna_award }
			GER = { has_war_with = POL }
		}
		country_exists = ENG
	}
	abort = {
		NOT = { country_exists = ENG }
	}
	
	ai_strategy = {
		type = befriend
		id = "ENG"			
		value = 200
	}
	ai_strategy = {
		type = alliance
		id = "ENG"			
		value = 200
	}	
}

i_SOV_case_white = {
	enable = {
		tag = POL
		OR = {
			GER = { has_wargoal_against = GER }
			GER = { is_justifying_wargoal_against = GER }
		}
		country_exists = ENG
	}
	abort = {
		NOT = { country_exists = ENG }
	}
	
	ai_strategy = {
		type = befriend
		id = "ENG"			
		value = 200
	}
	ai_strategy = {
		type = ignore
		id = "SOV"			
		value = 200
	}
	ai_strategy = {
		type = alliance
		id = "ENG"			
		value = 200
	}	
}

POL_stay_defensive = {
	enable = {
		tag = POL
		OR = {
			has_war_with = GER
			GER = { is_justifying_wargoal_against = POL }
		}
	}
	abort = {
		has_capitulated = yes
	}
	
	ai_strategy = {
		type = consider_weak
		id = "GER"			
		value = -300000
	}	
}