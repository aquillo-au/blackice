add_namespace = EAI_M

####################################################
# background events
####################################################

country_event = { #mobile warfare doctrine tech switch to manpower branch
	id = EAI_M.3
	hidden = yes
	mean_time_to_happen = { days = 2 }
	trigger = {
		is_ai = yes
		has_idea = cons_barrel
		has_manpower < 1000000
		has_tech = modern_blitzkrieg
		NOT = { has_country_flag = volkssturm_taken }
	}
	immediate = {
		set_country_flag = volkssturm_taken

		set_technology = { firebrigades = 0 }
		set_technology = { backhand_blow = 0 }
		set_technology = { modern_blitzkrieg = 0 }

		set_technology = { volkssturm = 1 }
	}
	option = {}
}

#Mobilization Law only for now
country_event = {
	id = EAI_M.1
	hidden = yes

	mean_time_to_happen = { days = 2 }

	trigger = {
		is_ai = yes
		has_war = yes
		NOT = { has_idea = mob_general }
	}
	
	option = { add_political_power = -25 add_ideas = mob_general }
}

#Automatically switch to next Economy law

country_event = { #Switch to low mobilisation from civilian economy
	id = EAI_M.100
	hidden = yes
	mean_time_to_happen = { days = 2 }
	trigger = {
		is_ai = yes
		has_idea = civilian_economy
		OR = {
			has_government = fascism
			has_government = communism
			has_war = yes
			threat > 0.15
		}
		if = {
			limit = {
				original_tag = HUN
				has_dlc = "Death or Dishonor"
			}
			NOT = { has_idea = HUN_treaty_of_triannon }
		}
	}
	immediate = {
		add_ideas = low_economic_mobilisation
		add_political_power = -150
	}
	option = {}
}

country_event = { #Switch to partial mobilisation from low mobilisation
	id = EAI_M.101
	hidden = yes
	mean_time_to_happen = { days = 2 }
	trigger = {
		is_ai = yes
		has_idea = low_economic_mobilisation
		OR = {
			has_government = fascism
			has_government = communism
			has_war = yes
			threat > 0.30
		}
		if = {
			limit = {
				original_tag = HUN
				has_dlc = "Death or Dishonor"
			}
			OR = {
				has_completed_focus = HUN_secret_rearmament
				NOT = { has_idea = HUN_treaty_of_triannon }
			}
		}
	}
	immediate = {
		add_ideas = partial_economic_mobilisation
		add_political_power = -150
	}
	option = {}
}

country_event = { #Switch to war economy from partial
	id = EAI_M.102
	hidden = yes
	mean_time_to_happen = { days = 2 }
	trigger = {
		is_ai = yes
		has_idea = partial_economic_mobilisation
		threat > 0.50
			OR = {
				has_government = fascism
				has_government = communism
				AND = {
					has_war = yes
					any_enemy_country = {
						ic_ratio = { 
							tag = ROOT 
							ratio > 0.1
						}
					}
				}
			}			
			if = {
				limit = {
					original_tag = HUN
					has_dlc = "Death or Dishonor"
				}
				NOT = { has_idea = HUN_treaty_of_triannon }
			}
	}
	immediate = {
		add_ideas = war_economy
		add_political_power = -150
	}
	option = {}
}



